/* Exercise Chapter 1.5. P.108. */

/* Exercise 1.5.1. Add user documentation to Listing 1.5. */

/* Exercise 1.5.2. a) use a while looop b) use a do while loop */

/** a) while loop
 * int i;
 * while (i < note) {
 * 	   intervals[i++] = frequency;
 *     frequency *= ratio;
 * };
 * 
 * i = 0;
 * while (i < note) {
 *     printf("%f\n", intervals[i];
 * };
 */

/** b) do while loop, relies on <= operator
 * int i;
 * do {
 *     intervals[i++] = frequency;
 *     frequency *= ratio;
 * } while (i <= 12);
 *
 * i = 0;
 * do {
 *     printf("%f\n", intervals[i]);
 * } while (i <= 12);
 */

/* Exercise 1.5.3. Modify Listing 1.5 to produce note+1 frequencies. */

/* Exercise 1.5.4. Modify Listing 1.5 to include counter at start of line. */

/* Exercise 1.5.5. Include scale as an argument in Listing 1.5. */

